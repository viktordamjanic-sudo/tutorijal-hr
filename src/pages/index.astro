---
// Main landing page for tutorijal.hr
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import ModuleGrid from '../components/ModuleGrid.astro';
import ConvexWrapper from '../components/ConvexWrapper.tsx';
import LiveFeedConvex from '../components/LiveFeedConvex.tsx';
import { SignedIn, SignedOut } from '@clerk/astro/components';
---

<Layout title="AI Tutorijal - Nauči AI riješiti stvarne probleme">
  <main class="min-h-screen bg-[#0a0a0c] selection:bg-indigo-500/30">
    <Hero />
    
    <!-- Live AI-Generated Tasks from News -->
    <section class="py-16 px-4 relative overflow-hidden bg-[#0a0a0c] border-t border-white/5 z-20">
      <div class="max-w-6xl mx-auto relative z-10">
        <div class="flex items-center gap-4 mb-4 justify-center md:justify-start">
          <span class="relative flex h-3 w-3">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-indigo-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-3 w-3 bg-indigo-500 shadow-[0_0_10px_rgba(99,102,241,0.5)]"></span>
          </span>
          <h2 class="text-3xl font-bold text-slate-100 font-['Outfit'] tracking-tight">Stvarni zadaci iz vijesti</h2>
        </div>
        <p class="text-slate-400 mb-10 max-w-3xl text-lg font-['Nunito'] text-center md:text-left leading-relaxed">
          Zadaci se učitavaju istog trena iz naše baze podataka. 
          <span class="text-indigo-400 font-semibold underline decoration-white/20 underline-offset-4">Zajedno rješavamo današnje probleme!</span>
        </p>
        
        <div class="bg-[#050508] border border-white/10 shadow-[0_0_40px_rgba(0,0,0,0.5)] p-2 md:p-6 overflow-hidden relative">
          <LiveFeedConvex client:only="react" />
        </div>
      </div>
    </section>

    <!-- Core Learning Modules -->
    <section class="py-16 px-4 bg-[#0a0a0c] relative border-t border-white/5">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
          <span class="inline-block px-4 py-1.5 border border-white/20 text-slate-300 text-xs font-bold tracking-[0.2em] uppercase mb-4 font-mono">Zabavno učenje</span>
          <h2 class="text-4xl md:text-5xl font-bold text-slate-100 mb-6 font-['Outfit'] tracking-tight">Izaberi svoju avanturu</h2>
          <p class="text-slate-400 text-xl max-w-2xl mx-auto font-['Nunito'] leading-relaxed">
            Svaki modul rješava jedan stvaran izazov. Od parkinga do padeža — 
            otkrij kako ti AI može biti najbolji pomoćnik!
          </p>
        </div>
        
        <ModuleGrid />
      </div>
    </section>

    <!-- Course Section -->
    <section class="py-24 px-4 bg-[#050508] text-white relative overflow-hidden border-y border-white/5">
      <div class="max-w-6xl mx-auto relative z-10">
        <div class="text-center mb-16">
          <span class="inline-block px-4 py-1.5 border border-white/20 bg-white/5 text-slate-300 text-xs font-bold tracking-[0.2em] uppercase mb-6 backdrop-blur-sm">Napredni Koncepti</span>
          <h2 class="text-4xl md:text-5xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-white to-white/60 mb-6 font-['Outfit'] tracking-tight">Moćni AI Agenti</h2>
          <p class="text-slate-400 text-xl max-w-2xl mx-auto font-['Nunito'] leading-[1.7]">
            Spreman za sljedeći korak? Nauči kako stvoriti vlastite AI radnike koji će umjesto tebe obavljati dosadne zadatke.
          </p>
        </div>
        
        <div class="grid grid-cols-2 md:grid-cols-5 gap-4 md:gap-6">
          {[
            { num: 1, title: "Prvi koraci", icon: "🚀", color: "white" },
            { num: 2, title: "Tvoj tim", icon: "👥", color: "indigo" },
            { num: 3, title: "Pametno suđenje", icon: "⚖️", color: "blue" },
            { num: 4, title: "Supermoći", icon: "🧰", color: "purple" },
            { num: 5, title: "Konačna magija", icon: "🎉", color: "fuchsia" },
          ].map((lesson, i) => (
            <a 
              href={`/lekcija/${lesson.num}`}
              class={`bg-[#0a0a0c] p-6 border border-white/10 hover:border-white/30 hover:bg-white/5 transition-all duration-300 group`}
            >
              <div class="text-3xl mb-4 text-slate-400 group-hover:text-white transition-colors">{lesson.icon}</div>
              <div class={`text-[10px] font-mono text-slate-500 mb-2 tracking-[0.2em] uppercase`}>Nivo {lesson.num}</div>
              <div class="font-bold text-lg text-slate-200 group-hover:text-white transition-colors font-['Outfit']">
                {lesson.title}
              </div>
            </a>
          ))}
        </div>
        
        <div class="text-center mt-16">
          <SignedIn>
            <a 
              href="/lekcija/1" 
              class="inline-flex items-center justify-center gap-3 bg-white text-slate-900 px-10 py-5 rounded-2xl font-bold text-lg lg:text-xl shadow-[0_0_30px_rgba(255,255,255,0.15)] hover:shadow-[0_0_40px_rgba(255,255,255,0.3)] hover:-translate-y-1 transition-all duration-300 transform"
            >
              🚀 Nastavi rad u terminalu
            </a>
          </SignedIn>
          <SignedOut>
            <a 
              href="/sign-up" 
              class="inline-flex items-center justify-center gap-3 bg-white text-slate-900 px-10 py-5 rounded-2xl font-bold text-lg lg:text-xl shadow-[0_0_30px_rgba(255,255,255,0.15)] hover:shadow-[0_0_40px_rgba(255,255,255,0.3)] hover:-translate-y-1 transition-all duration-300 transform"
            >
              🚀 Otključaj napredne alate
            </a>
            <p class="mt-6 text-slate-400 font-medium">
              Imaš pristup? <a href="/sign-in" class="text-indigo-400 hover:text-indigo-300 font-bold hover:underline decoration-2 underline-offset-4 transition-colors">Prijavi se ovdje</a>
            </p>
          </SignedOut>
          <SignedIn>
            <a 
              href="/lekcija/1" 
              class="inline-flex items-center justify-center gap-3 bg-gradient-to-r from-purple-600 to-pink-500 text-white px-10 py-5 rounded-2xl font-bold text-lg lg:text-xl shadow-[0_8px_30px_rgba(236,72,153,0.3)] hover:shadow-[0_8px_30px_rgba(236,72,153,0.5)] hover:-translate-y-1 transition-all"
            >
              🚀 Nastavi tamo gdje si stao
            </a>
          </SignedIn>
        </div>
      </div>
    </section>

    <!-- How It Works (Dark Studio style) -->
    <section class="py-24 px-4 bg-[#0a0a0c] relative overflow-hidden border-b border-white/5">
      <div class="max-w-5xl mx-auto text-center relative z-10">
        <h2 class="text-4xl md:text-5xl font-bold mb-16 font-['Outfit'] tracking-tight text-white">Način rada <span class="text-slate-500 tracking-normal font-mono text-2xl mix-blend-screen">// SYS_FLOW</span></h2>
        <div class="grid md:grid-cols-4 gap-4 md:gap-6">
          {[
            { tag: 'SYS.01', title: 'Problem', desc: 'Identificiraj izazov iz stvarnog svijeta' },
            { tag: 'SYS.02', title: 'Kontekst', desc: 'Analiziraj kako stručnjaci pristupaju zadatku' },
            { tag: 'SYS.03', title: 'Izvršenje', desc: 'Upotrijebi AI terminal za rješavanje' },
            { tag: 'SYS.04', title: 'Memorija', desc: 'Spremi proces za buduće ponavljanje' }
          ].map((step) => (
            <div class={`bg-[#050508] p-6 border border-white/10 hover:border-white/30 transition-colors text-left group`}>
              <div class="font-mono text-[10px] text-indigo-400 mb-4 tracking-[0.2em]">{step.tag}</div>
              <h3 class="font-bold text-xl mb-2 text-slate-200 font-['Outfit'] tracking-tight">{step.title}</h3>
              <p class="text-slate-500 font-['Nunito'] leading-relaxed text-sm">{step.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- For Everyone (Dark Mode variant) -->
    <section class="py-24 px-4 bg-[#050508]">
      <div class="max-w-6xl mx-auto grid md:grid-cols-2 gap-6 relative z-10">
        <!-- For Seniors -->
        <div class="bg-[#0a0a0c] p-10 border border-white/5 hover:border-white/20 transition-colors">
          <div class="font-mono text-[10px] tracking-[0.2em] text-amber-500 mb-6 uppercase">Pristupačnost</div>
          <h3 class="text-3xl font-bold mb-6 text-white font-['Outfit']">Prijateljsko za starije</h3>
          <ul class="space-y-4 text-slate-400 font-['Nunito'] text-base">
            <li class="flex items-center gap-4">
              <span class="text-amber-500 font-bold">✓</span> Velika slova i jasnije boje
            </li>
            <li class="flex items-center gap-4">
              <span class="text-amber-500 font-bold">✓</span> Glasovna pomoć (AI čita naglas)
            </li>
            <li class="flex items-center gap-4">
              <span class="text-amber-500 font-bold">✓</span> Bez teških engleskih riječi
            </li>
            <li class="flex items-center gap-4">
              <span class="text-amber-500 font-bold">✓</span> Situacije iz stvarnog života
            </li>
          </ul>
        </div>

        <!-- For Kids -->
        <div class="bg-[#0a0a0c] p-10 border border-white/5 hover:border-white/20 transition-colors">
          <div class="font-mono text-[10px] tracking-[0.2em] text-emerald-500 mb-6 uppercase">Edukacija</div>
          <h3 class="text-3xl font-bold mb-6 text-white font-['Outfit']">Zabavno za mlađe</h3>
          <ul class="space-y-4 text-slate-400 font-['Nunito'] text-base">
            <li class="flex items-center gap-4">
              <span class="text-emerald-500 font-bold">★</span> Slatke ilustracije i animacije
            </li>
            <li class="flex items-center gap-4">
              <span class="text-emerald-500 font-bold">★</span> Sakupljanje bodova i pehara
            </li>
            <li class="flex items-center gap-4">
              <span class="text-emerald-500 font-bold">★</span> Kratke misije (gotove za 2 min)
            </li>
            <li class="flex items-center gap-4">
              <span class="text-emerald-500 font-bold">★</span> Školska gradiva kroz igru
            </li>
          </ul>
        </div>
      </div>
    </section>
  </main>
</Layout>
