---
// Base Layout Component
import { SignedIn, SignedOut, UserButton } from '@clerk/astro/components';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="hr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content="Nauči koristiti AI za rješavanje stvarnih problema - od parkinga do padeža!" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    
    <!-- Fonts - Modern, playful and highly readable -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&family=Outfit:wght@400;500;700;800&display=swap" rel="stylesheet">
  </head>
  <body class="font-sans antialiased text-slate-300 bg-[#0a0a0c] selection:bg-indigo-500/30 scroll-smooth">
      <header class="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
          <a href="/" class="flex items-center gap-2">
            <span class="text-3xl">🎓</span>
            <div>
              <h1 class="font-bold text-xl text-white">tutorijal.hr</h1>
              <p class="text-xs text-slate-400 font-mono tracking-wider">AI TOOLS // 2026</p>
            </div>
          </a>
          
          <!-- Desktop Navigation -->
          <nav class="hidden md:flex items-center gap-6">
            <a href="/" class="text-slate-300 font-medium hover:text-white transition">Početna</a>
            <a href="/#moduli" class="text-slate-300 font-medium hover:text-white transition">Moduli</a>
            <a href="/lekcija/1" class="text-slate-300 font-medium hover:text-white transition">Lekcije</a>

            <SignedIn>
              <a href="/profil" class="text-slate-300 font-medium hover:text-white transition">Profil</a>
              <UserButton afterSignOutUrl="/" />
            </SignedIn>

            <SignedOut>
              <a
                href="/sign-in"
                class="bg-white/10 text-white border border-white/20 px-4 py-2 rounded-lg hover:bg-white/20 transition backdrop-blur-md"
              >
                Prijava
              </a>
            </SignedOut>
          </nav>

          <!-- Mobile Menu Button -->
          <button
            id="mobile-menu-btn"
            class="md:hidden p-2 rounded-lg text-slate-300 hover:bg-white/10 transition"
            aria-label="Otvori izbornik"
            aria-expanded="false"
            onclick="document.getElementById('mobile-menu').classList.toggle('hidden'); this.setAttribute('aria-expanded', document.getElementById('mobile-menu').classList.contains('hidden') ? 'false' : 'true'); document.getElementById('menu-icon').classList.toggle('hidden'); document.getElementById('close-icon').classList.toggle('hidden');"
          >
            <svg id="menu-icon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg id="close-icon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <!-- Mobile Navigation -->
        <nav
          id="mobile-menu"
          class="hidden md:hidden border-t border-white/10 bg-[#0a0a0c]/95 backdrop-blur-xl absolute w-full"
        >
          <div class="max-w-6xl mx-auto px-4 py-4 flex flex-col gap-4">
            <a href="/" class="text-slate-300 hover:text-white transition py-2 border-b border-white/5">Početna</a>
            <a href="/#moduli" class="text-slate-300 hover:text-white transition py-2 border-b border-white/5">Moduli</a>
            <a href="/lekcija/1" class="text-slate-300 hover:text-white transition py-2 border-b border-white/5">Lekcije</a>

            <SignedIn>
              <a href="/profil" class="text-slate-300 hover:text-white transition py-2">Profil</a>
              <div class="py-2">
                <UserButton afterSignOutUrl="/" />
              </div>
            </SignedIn>

            <SignedOut>
              <a
                href="/sign-in"
                class="bg-white/10 text-white border border-white/20 px-4 py-3 rounded-lg hover:bg-white/20 transition text-center font-medium"
              >
                Prijava
              </a>
            </SignedOut>
          </div>
        </nav>
      </header>

      <slot />

      <footer class="bg-[#050508] border-t border-white/5 text-slate-400 py-12">
        <div class="max-w-6xl mx-auto px-4">
          <div class="grid md:grid-cols-3 gap-8">
            <div>
              <h3 class="font-bold text-white text-lg mb-4">🎓 tutorijal.hr</h3>
              <p class="text-sm border-l-2 border-indigo-500/50 pl-3">
                Učimo ljude svih uzrasta kako AI može pomoći u svakodnevnom životu.
              </p>
            </div>
            
            <div>
              <h4 class="font-semibold text-white mb-4">Brzi linkovi</h4>
              <ul class="space-y-2 text-sm">
                <li><a href="/" class="hover:text-white transition">Početna</a></li>
                <li><a href="/#moduli" class="hover:text-white transition">Moduli</a></li>
                <li><a href="/lekcija/1" class="hover:text-white transition">Lekcije</a></li>
              </ul>
            </div>
            
            <div>
              <h4 class="font-semibold text-white mb-4">Kontakt</h4>
              <p class="text-sm font-mono opacity-80">
                sys.mail: info@tutorijal.hr<br />
                sys.loc: Zagreb, Hrvatska
              </p>
            </div>
          </div>
          
          <div class="border-t border-white/5 mt-8 pt-8 text-center text-sm font-mono opacity-60">
            © 2026 tutorijal.hr — Sva prava pridržana
          </div>
        </div>
      </footer>
  </body>
</html>
